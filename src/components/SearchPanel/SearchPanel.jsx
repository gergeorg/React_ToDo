import React from 'react'
import './SearchPanel.scss'

const SearchPanel = () => {
  return <input className="form-control SearchPanel" placeholder="What needs to be done?"/>
}

export default SearchPanel
